# biotrack_proj
 Develop the "Virtual Sensor" suite. Creates background workers that simulate real-world hardware failures (e.g., a fridge compressor failing) and trigger instant emergency notifications.
